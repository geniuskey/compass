# @package _global_
solver:
  name: "grcwa"
  type: "rcwa"

  params:
    nG: 49                     # total plane-wave count (grcwa semantics, NOT per-axis order)
    dtype: "complex128"
    n_lens_slices: 30
    grid_multiplier: 3
